{
  "name": "@triveni/pricing-preview",
  "version": "1.0.0",
  "private": true,
  "description": "Layer-1 instant estimate logic — shared between web and mobile",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@triveni/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}

{
  "name": "@triveni/shared-validation",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Zod validation schemas — same validation client and server side",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@triveni/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
